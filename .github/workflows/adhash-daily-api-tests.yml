name: AdHash Daily API Tests

on:
  schedule:
    # Runs EVERY DAY at 9:00 AM IST (3:30 AM UTC)
    - cron: '30 3 * * *'
    # Runs EVERY DAY at 5:00 PM IST (11:30 AM UTC)
    - cron: '30 11 * * *'
  workflow_dispatch:
    # Allows manual triggering of the workflow

jobs:
  api-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 15  # Max 15 minutes to prevent hanging (tests normally complete in ~2 mins)

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up JDK 11
      uses: actions/setup-java@v4
      with:
        java-version: '11'
        distribution: 'temurin'

    - name: Cache Maven dependencies
      uses: actions/cache@v4
      with:
        path: ~/.m2
        key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
        restore-keys: ${{ runner.os }}-m2

    - name: Display current IST time
      run: |
        echo "Current UTC time: $(date -u)"
        echo "Current IST time: $(TZ='Asia/Kolkata' date)"
        echo "Workflow triggered for AdHash API tests (Weekends & Holidays 2026)"
        
    - name: Run AdHash API Tests
      run: |
        echo "Starting AdHash Daily API Tests..."
        echo "Running all 75 API tests for AdHash projects"
        mvn test -Dsurefire.suiteXmlFiles=TestNG.xml -DfailIfNoTests=false
        
    - name: Upload test results
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: adhash-api-test-results
        path: |
          target/surefire-reports/
          target/test-classes/
        retention-days: 30
        
    - name: Display test summary
      if: always()
      run: |
        echo "=== AdHash API Tests Summary ==="
        echo "Date: $(TZ='Asia/Kolkata' date '+%d/%m/%Y %H:%M IST')"
        echo "Repository: AdHash-API-Test"
        echo "Test Suite: TestNG.xml"
        echo "Expected Tests: 75 (All AdHash Projects API Tests)"
        echo "Test Class: generated.GeneratedApiTest"
        echo "============================================="
        
    - name: Parse test results
      if: always()
      id: test-results
      run: |
        # Initialize variables
        TESTS_RUN=0
        FAILURES=0
        ERRORS=0
        SKIPPED=0
        STATUS="UNKNOWN"

        # Parse TestNG results if available
        if [ -f target/surefire-reports/testng-results.xml ]; then
          echo "Parsing TestNG results..."
          TESTS_RUN=$(grep -o 'total="[0-9]*"' target/surefire-reports/testng-results.xml | grep -o '[0-9]*' || echo "0")
          FAILURES=$(grep -o 'failed="[0-9]*"' target/surefire-reports/testng-results.xml | grep -o '[0-9]*' || echo "0")
          ERRORS=$(grep -o 'errors="[0-9]*"' target/surefire-reports/testng-results.xml | grep -o '[0-9]*' || echo "0")
          SKIPPED=$(grep -o 'skipped="[0-9]*"' target/surefire-reports/testng-results.xml | grep -o '[0-9]*' || echo "0")
        fi

        # Determine overall status
        if [ "$FAILURES" -eq 0 ] && [ "$ERRORS" -eq 0 ] && [ "$TESTS_RUN" -gt 0 ]; then
          STATUS="SUCCESS"
        elif [ "$TESTS_RUN" -eq 0 ]; then
          STATUS="NO_TESTS"
        else
          STATUS="FAILURE"
        fi

        # Set outputs for email step
        echo "tests_run=$TESTS_RUN" >> $GITHUB_OUTPUT
        echo "failures=$FAILURES" >> $GITHUB_OUTPUT
        echo "errors=$ERRORS" >> $GITHUB_OUTPUT
        echo "skipped=$SKIPPED" >> $GITHUB_OUTPUT
        echo "status=$STATUS" >> $GITHUB_OUTPUT
        echo "timestamp=$(TZ='Asia/Kolkata' date '+%d/%m/%Y %H:%M IST')" >> $GITHUB_OUTPUT

        echo "Test Results Summary:"
        echo "Tests Run: $TESTS_RUN"
        echo "Failures: $FAILURES"
        echo "Errors: $ERRORS"
        echo "Skipped: $SKIPPED"
        echo "Status: $STATUS"

    - name: Calculate Test Statistics
      if: always()
      id: test-stats
      shell: bash
      run: |
        TESTS_RUN="${{ steps.test-results.outputs.tests_run }}"
        FAILURES="${{ steps.test-results.outputs.failures }}"
        ERRORS="${{ steps.test-results.outputs.errors }}"

        # Calculate passed tests
        PASSED=$((TESTS_RUN - FAILURES - ERRORS))
        FAILED=$((FAILURES + ERRORS))

        # Calculate success rate
        if [ "$TESTS_RUN" -gt 0 ]; then
          SUCCESS_RATE=$(awk "BEGIN {printf \"%.1f\", ($PASSED * 100.0 / $TESTS_RUN)}")
        else
          SUCCESS_RATE="0.0"
        fi

        echo "passed=$PASSED" >> $GITHUB_OUTPUT
        echo "failed=$FAILED" >> $GITHUB_OUTPUT
        echo "success_rate=$SUCCESS_RATE" >> $GITHUB_OUTPUT

        echo "Calculated Statistics:"
        echo "Passed: $PASSED"
        echo "Failed: $FAILED"
        echo "Success Rate: $SUCCESS_RATE%"

    - name: Extract Individual Test Results
      if: always()
      id: extract-tests
      shell: bash
      run: |
        echo "Extracting individual test results..."

        # Create a temporary file for test details
        TEST_DETAILS_FILE="test_details.html"

        if [ -f target/surefire-reports/testng-results.xml ]; then
          # Parse TestNG XML and extract test methods
          python3 << 'EOF'
        import xml.etree.ElementTree as ET
        import html

        try:
            tree = ET.parse('target/surefire-reports/testng-results.xml')
            root = tree.getroot()

            test_rows = []
            test_count = 0

            # Find all test methods
            for test_method in root.findall('.//test-method[@is-config="false"]'):
                test_count += 1
                name = test_method.get('name', 'Unknown')
                status = test_method.get('status', 'UNKNOWN')

                # Clean up test name - remove "test" prefix and make readable
                display_name = name.replace('test', '').replace('_', ' ').strip()
                if not display_name:
                    display_name = name

                # Determine status icon and color
                if status == 'PASS':
                    icon = '✅'
                    status_text = 'PASS'
                    row_class = 'pass'
                    status_color = '#4caf50'
                elif status == 'FAIL':
                    icon = '❌'
                    status_text = 'FAIL'
                    row_class = 'fail'
                    status_color = '#f44336'
                else:
                    icon = '⚠️'
                    status_text = 'SKIP'
                    row_class = 'skip'
                    status_color = '#ff9800'

                # Create table row
                row = f'''
                  <tr class="{row_class}">
                    <td style="padding: 10px; text-align: center; border-bottom: 1px solid #e0e0e0;">{test_count}</td>
                    <td style="padding: 10px; border-bottom: 1px solid #e0e0e0;">{html.escape(display_name)}</td>
                    <td style="padding: 10px; text-align: center; border-bottom: 1px solid #e0e0e0; color: {status_color}; font-weight: bold;">{icon} {status_text}</td>
                  </tr>'''
                test_rows.append(row)

            # Write to file
            with open('test_details.html', 'w', encoding='utf-8') as f:
                f.write('\n'.join(test_rows))

            print(f"Extracted {test_count} test results")

        except Exception as e:
            print(f"Error parsing test results: {e}")
            with open('test_details.html', 'w', encoding='utf-8') as f:
                f.write('<tr><td colspan="3" style="padding: 20px; text-align: center; color: #666;">No test details available</td></tr>')
        EOF

          echo "test_details_available=true" >> $GITHUB_OUTPUT
        else
          echo "TestNG results file not found"
          echo '<tr><td colspan="3" style="padding: 20px; text-align: center; color: #666;">No test results found</td></tr>' > test_details.html
          echo "test_details_available=false" >> $GITHUB_OUTPUT
        fi

    - name: Find HTML Report
      if: always()
      id: find-report
      shell: bash
      run: |
        echo "🔍 Looking for HTML test report..."

        # Find the most recent HTML report in simple-reports directory
        if [ -d "simple-reports" ]; then
          REPORT_FILE=$(ls -t simple-reports/AdHash_API_Simple_Report_*.html 2>/dev/null | head -n 1)
          if [ -n "$REPORT_FILE" ]; then
            echo "report_path=$REPORT_FILE" >> $GITHUB_OUTPUT
            echo "report_found=true" >> $GITHUB_OUTPUT
            echo "✅ Found HTML report: $REPORT_FILE"

            # Get file size for logging
            FILE_SIZE=$(du -h "$REPORT_FILE" | cut -f1)
            echo "📊 Report size: $FILE_SIZE"
          else
            echo "report_found=false" >> $GITHUB_OUTPUT
            echo "⚠️ No HTML report found in simple-reports directory"
          fi
        else
          echo "report_found=false" >> $GITHUB_OUTPUT
          echo "⚠️ simple-reports directory not found"
        fi

    - name: Check Email Secrets
      if: always()
      id: check-secrets
      run: |
        if [ -z "${{ secrets.EMAIL_USERNAME }}" ] || [ -z "${{ secrets.EMAIL_PASSWORD }}" ] || [ -z "${{ secrets.EMAIL_TO }}" ]; then
          echo "secrets_available=false" >> $GITHUB_OUTPUT
          echo "⚠️ Email secrets are not configured. Skipping email notifications."
          echo "Please add EMAIL_USERNAME, EMAIL_PASSWORD, and EMAIL_TO secrets to the repository."
        else
          echo "secrets_available=true" >> $GITHUB_OUTPUT
          echo "✅ Email secrets are configured."
        fi

    - name: Send Success Email
      if: always() && steps.test-results.outputs.status == 'SUCCESS' && steps.check-secrets.outputs.secrets_available == 'true'
      shell: bash
      run: |
        # Extract SSL issues from HTML report and send email with card-based UI
        python3 << 'PYEOF'
        import smtplib
        import re
        import os
        from email.mime.multipart import MIMEMultipart
        from email.mime.text import MIMEText
        from email.mime.base import MIMEBase
        from email import encoders

        # Extract SSL issues from the HTML report
        ssl_issues = []
        report_path = "${{ steps.find-report.outputs.report_path }}"
        if report_path and os.path.exists(report_path):
            with open(report_path, 'r', encoding='utf-8') as f:
                content = f.read()
                ssl_match = re.search(r'SSL Certificate Issues Detected.*?<ul.*?>(.*?)</ul>', content, re.DOTALL)
                if ssl_match:
                    items = re.findall(r'<li.*?><strong>(.*?)</strong>.*?<a href="(.*?)"', ssl_match.group(1), re.DOTALL)
                    ssl_issues = [(name.strip(), url.strip()) for name, url in items]

        # Build SSL section HTML
        ssl_html = ""
        if ssl_issues:
            ssl_items = ""
            for name, url in ssl_issues:
                ssl_items += f'<div style="padding:8px 0;border-bottom:1px solid #FFE0B2;"><strong style="color:#E65100;">⚠ {name}</strong><br><a href="{url}" style="color:#FF6D00;font-size:12px;word-break:break-all;">{url}</a></div>'
            ssl_html = f'''
            <div style="background:#FFF3E0;border-radius:8px;padding:15px;margin-top:15px;border-left:4px solid #FF9800;">
              <div style="font-weight:bold;color:#E65100;margin-bottom:10px;">🔒 SSL CERTIFICATE WARNING - {len(ssl_issues)} Issue(s)</div>
              {ssl_items}
            </div>'''

        # Build HTML email body with card-based UI
        html_body = f'''<!DOCTYPE html>
<html><head><meta charset="UTF-8"></head>
<body style="margin:0;padding:20px;background:#f5f5f5;font-family:Arial,sans-serif;">
<table width="600" align="center" cellpadding="0" cellspacing="0" style="background:#fff;border-radius:12px;box-shadow:0 2px 8px rgba(0,0,0,0.1);">

<!-- Header -->
<tr><td style="background:linear-gradient(135deg,#4CAF50,#2E7D32);padding:25px;text-align:center;border-radius:12px 12px 0 0;">
<h1 style="margin:0;color:#fff;font-size:22px;">🎯 AdHash API Test Report</h1>
<p style="margin:8px 0 0;color:rgba(255,255,255,0.9);font-size:13px;">${{ steps.test-results.outputs.timestamp }}</p>
</td></tr>

<!-- Stats Cards -->
<tr><td style="padding:20px;">
<table width="100%" cellpadding="0" cellspacing="8">
<tr>
<td width="33%" align="center" style="background:#E3F2FD;padding:15px;border-radius:8px;">
<div style="font-size:28px;font-weight:bold;color:#1976D2;">${{ steps.test-results.outputs.tests_run }}</div>
<div style="font-size:11px;color:#666;margin-top:4px;">Total Tests</div></td>
<td width="33%" align="center" style="background:#E8F5E9;padding:15px;border-radius:8px;">
<div style="font-size:28px;font-weight:bold;color:#388E3C;">✓ ${{ steps.test-stats.outputs.passed }}</div>
<div style="font-size:11px;color:#666;margin-top:4px;">Passed</div></td>
<td width="33%" align="center" style="background:#FFEBEE;padding:15px;border-radius:8px;">
<div style="font-size:28px;font-weight:bold;color:#D32F2F;">✗ ${{ steps.test-stats.outputs.failed }}</div>
<div style="font-size:11px;color:#666;margin-top:4px;">Failed</div></td>
</tr></table>

<!-- Success Rate & Status -->
<div style="text-align:center;margin:20px 0;">
<div style="font-size:42px;font-weight:bold;color:#4CAF50;">${{ steps.test-stats.outputs.success_rate }}%</div>
<div style="font-size:12px;color:#666;">Success Rate</div>
</div>

<div style="background:#4CAF50;color:#fff;text-align:center;padding:12px;border-radius:8px;font-weight:bold;font-size:14px;">
✓ ALL TESTS PASSED
</div>

<!-- SSL Warning (if any) -->
{ssl_html}

<!-- Attachment Note -->
<div style="background:#E8F5E9;border-radius:8px;padding:12px;margin-top:15px;border-left:4px solid #4CAF50;">
<strong style="color:#2E7D32;">📎 Detailed report attached</strong>
<div style="font-size:12px;color:#558B2F;margin-top:4px;">Open the HTML attachment for complete test details.</div>
</div>

</td></tr>

<!-- Footer -->
<tr><td style="background:#FAFAFA;padding:15px;text-align:center;border-radius:0 0 12px 12px;border-top:1px solid #EEE;">
<p style="margin:0;font-size:11px;color:#999;">AdHash API Test Automation | ${{ steps.test-results.outputs.timestamp }}</p>
</td></tr>

</table></body></html>'''

        # Email configuration
        smtp_server = "smtp.zoho.com"
        smtp_port = 465
        username = "${{ secrets.EMAIL_USERNAME }}"
        password = "${{ secrets.EMAIL_PASSWORD }}"
        from_email = "${{ secrets.EMAIL_USERNAME }}"
        to_email = "${{ secrets.EMAIL_TO }}"
        subject = "✅ AdHash Daily API Tests - SUCCESS (${{ steps.test-results.outputs.timestamp }})"

        # Create message
        msg = MIMEMultipart()
        msg['From'] = from_email
        msg['To'] = to_email
        msg['Subject'] = subject
        msg.attach(MIMEText(html_body, 'html'))

        # Attach HTML report if available
        if report_path and os.path.exists(report_path):
            with open(report_path, 'rb') as f:
                part = MIMEBase('application', 'octet-stream')
                part.set_payload(f.read())
                encoders.encode_base64(part)
                part.add_header('Content-Disposition', f'attachment; filename="{os.path.basename(report_path)}"')
                msg.attach(part)

        # Send email
        try:
            with smtplib.SMTP_SSL(smtp_server, smtp_port) as server:
                server.login(username, password)
                server.send_message(msg)
            print("Success email sent successfully!")
        except Exception as e:
            print(f"Error sending email: {e}")
            raise
        PYEOF

    - name: Send Failure Email
      if: always() && (steps.test-results.outputs.status == 'FAILURE' || steps.test-results.outputs.status == 'NO_TESTS') && steps.check-secrets.outputs.secrets_available == 'true'
      shell: bash
      run: |
        # Extract SSL issues from HTML report and send email with card-based UI
        python3 << 'PYEOF'
        import smtplib
        import re
        import os
        from email.mime.multipart import MIMEMultipart
        from email.mime.text import MIMEText
        from email.mime.base import MIMEBase
        from email import encoders

        # Extract SSL issues from the HTML report
        ssl_issues = []
        report_path = "${{ steps.find-report.outputs.report_path }}"
        if report_path and os.path.exists(report_path):
            with open(report_path, 'r', encoding='utf-8') as f:
                content = f.read()
                ssl_match = re.search(r'SSL Certificate Issues Detected.*?<ul.*?>(.*?)</ul>', content, re.DOTALL)
                if ssl_match:
                    items = re.findall(r'<li.*?><strong>(.*?)</strong>.*?<a href="(.*?)"', ssl_match.group(1), re.DOTALL)
                    ssl_issues = [(name.strip(), url.strip()) for name, url in items]

        # Build SSL section HTML
        ssl_html = ""
        if ssl_issues:
            ssl_items = ""
            for name, url in ssl_issues:
                ssl_items += f'<div style="padding:8px 0;border-bottom:1px solid #FFE0B2;"><strong style="color:#E65100;">⚠ {name}</strong><br><a href="{url}" style="color:#FF6D00;font-size:12px;word-break:break-all;">{url}</a></div>'
            ssl_html = f'''
            <div style="background:#FFF3E0;border-radius:8px;padding:15px;margin-top:15px;border-left:4px solid #FF9800;">
              <div style="font-weight:bold;color:#E65100;margin-bottom:10px;">🔒 SSL CERTIFICATE WARNING - {len(ssl_issues)} Issue(s)</div>
              {ssl_items}
            </div>'''

        # Build HTML email body with card-based UI
        html_body = f'''<!DOCTYPE html>
<html><head><meta charset="UTF-8"></head>
<body style="margin:0;padding:20px;background:#f5f5f5;font-family:Arial,sans-serif;">
<table width="600" align="center" cellpadding="0" cellspacing="0" style="background:#fff;border-radius:12px;box-shadow:0 2px 8px rgba(0,0,0,0.1);">

<!-- Header -->
<tr><td style="background:linear-gradient(135deg,#F44336,#C62828);padding:25px;text-align:center;border-radius:12px 12px 0 0;">
<h1 style="margin:0;color:#fff;font-size:22px;">🎯 AdHash API Test Report</h1>
<p style="margin:8px 0 0;color:rgba(255,255,255,0.9);font-size:13px;">${{ steps.test-results.outputs.timestamp }}</p>
</td></tr>

<!-- Stats Cards -->
<tr><td style="padding:20px;">
<table width="100%" cellpadding="0" cellspacing="8">
<tr>
<td width="33%" align="center" style="background:#E3F2FD;padding:15px;border-radius:8px;">
<div style="font-size:28px;font-weight:bold;color:#1976D2;">${{ steps.test-results.outputs.tests_run }}</div>
<div style="font-size:11px;color:#666;margin-top:4px;">Total Tests</div></td>
<td width="33%" align="center" style="background:#E8F5E9;padding:15px;border-radius:8px;">
<div style="font-size:28px;font-weight:bold;color:#388E3C;">✓ ${{ steps.test-stats.outputs.passed }}</div>
<div style="font-size:11px;color:#666;margin-top:4px;">Passed</div></td>
<td width="33%" align="center" style="background:#FFEBEE;padding:15px;border-radius:8px;">
<div style="font-size:28px;font-weight:bold;color:#D32F2F;">✗ ${{ steps.test-stats.outputs.failed }}</div>
<div style="font-size:11px;color:#666;margin-top:4px;">Failed</div></td>
</tr></table>

<!-- Success Rate & Status -->
<div style="text-align:center;margin:20px 0;">
<div style="font-size:42px;font-weight:bold;color:#F44336;">${{ steps.test-stats.outputs.success_rate }}%</div>
<div style="font-size:12px;color:#666;">Success Rate</div>
</div>

<div style="background:#F44336;color:#fff;text-align:center;padding:12px;border-radius:8px;font-weight:bold;font-size:14px;">
✗ TESTS FAILED
</div>

<!-- SSL Warning (if any) -->
{ssl_html}

<!-- Attachment Note -->
<div style="background:#FFEBEE;border-radius:8px;padding:12px;margin-top:15px;border-left:4px solid #F44336;">
<strong style="color:#C62828;">📎 Detailed report attached</strong>
<div style="font-size:12px;color:#D32F2F;margin-top:4px;">Open the HTML attachment for complete test details and error information.</div>
</div>

</td></tr>

<!-- Footer -->
<tr><td style="background:#FAFAFA;padding:15px;text-align:center;border-radius:0 0 12px 12px;border-top:1px solid #EEE;">
<p style="margin:0;font-size:11px;color:#999;">AdHash API Test Automation | ${{ steps.test-results.outputs.timestamp }}</p>
</td></tr>

</table></body></html>'''

        # Email configuration
        smtp_server = "smtp.zoho.com"
        smtp_port = 465
        username = "${{ secrets.EMAIL_USERNAME }}"
        password = "${{ secrets.EMAIL_PASSWORD }}"
        from_email = "${{ secrets.EMAIL_USERNAME }}"
        to_email = "${{ secrets.EMAIL_TO }}"
        subject = "❌ AdHash Daily API Tests - FAILED (${{ steps.test-results.outputs.timestamp }})"

        # Create message
        msg = MIMEMultipart()
        msg['From'] = from_email
        msg['To'] = to_email
        msg['Subject'] = subject
        msg.attach(MIMEText(html_body, 'html'))

        # Attach HTML report if available
        if report_path and os.path.exists(report_path):
            with open(report_path, 'rb') as f:
                part = MIMEBase('application', 'octet-stream')
                part.set_payload(f.read())
                encoders.encode_base64(part)
                part.add_header('Content-Disposition', f'attachment; filename="{os.path.basename(report_path)}"')
                msg.attach(part)

        # Send email
        try:
            with smtplib.SMTP_SSL(smtp_server, smtp_port) as server:
                server.login(username, password)
                server.send_message(msg)
            print("Failure email sent successfully!")
        except Exception as e:
            print(f"Error sending email: {e}")
            raise
        PYEOF

    - name: Warn about missing secrets
      if: always() && steps.check-secrets.outputs.secrets_available == 'false'
      run: |
        echo "⚠️⚠️⚠️ WARNING: Email secrets are not configured! ⚠️⚠️⚠️"
        echo ""
        echo "To receive email notifications, please add these secrets to the repository:"
        echo "1. Go to: https://github.com/Thenilavan-G/AdHash-API-Test/settings/secrets/actions"
        echo "2. Add the following secrets:"
        echo "   - EMAIL_USERNAME: Your Zoho email address"
        echo "   - EMAIL_PASSWORD: Your Zoho email password"
        echo "   - EMAIL_TO: Recipient email address"
        echo ""
        echo "Without these secrets, the workflow will run but won't send email notifications."

    - name: Notify on failure (Console)
      if: failure()
      run: |
        echo "❌ AdHash API Tests workflow failed!"
        echo "Please check the logs above for details."

